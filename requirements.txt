# The default benchmark path uses only the Python standard library.
# Install these optional packages for notebook analysis or alternate plotting.
pyyaml>=6.0
matplotlib>=3.8
pandas>=2.0
pytest>=8.0

