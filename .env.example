# Optional: only needed when adding real external backends.
# The default benchmark in configs/default.yaml runs without any environment variables.

# OpenAI-compatible generation or embedding backend
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_GENERATION_MODEL=gpt-4.1-mini

# Vector database backends
PINECONE_API_KEY=
PINECONE_INDEX=
WEAVIATE_URL=
WEAVIATE_API_KEY=
QDRANT_URL=
QDRANT_API_KEY=

# Experiment tracking / reproducibility
MLFLOW_TRACKING_URI=
RAG_BENCHMARK_SEED=42
